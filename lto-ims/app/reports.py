# reports.py
# Wraps all 7 required SQL report queries

# TODO: report_drivers_filtered()         — filter by license type, status, age range, sex
# TODO: report_vehicles_by_driver()       — all vehicles owned by a given driver
# TODO: report_expired_registrations()    — vehicles with expired registrations as of a given date
# TODO: report_expired_suspended_licenses() — drivers with expired or suspended licenses
# TODO: report_violations_by_driver()     — violations by a driver within a date range
# TODO: report_violations_per_type()      — total violations per type for a given year
# TODO: report_vehicles_in_violations_by_location() — vehicles involved in violations by city/region
